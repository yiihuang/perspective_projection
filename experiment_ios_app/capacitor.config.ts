import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yiihuang.perspective',
  appName: 'Perspective Projection',
  webDir: 'dist'
};

export default config;
